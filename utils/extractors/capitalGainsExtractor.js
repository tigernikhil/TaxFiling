const extractCapitalGains = async (excelBuffer) => {
  const extractedData = {
    incomeDetails: {
      capitalGains: 0
    },
    source: 'Capital Gains'
  };
  return extractedData;
};

module.exports = { extractCapitalGains };